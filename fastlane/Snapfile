devices([
  "iPhone 16 Pro Max",
  "iPhone 16 Plus",
  "iPad Pro 13-inch (M4)"
])

languages(["en-US"])

scheme("Chess4UUITests")

output_directory("./fastlane/screenshots")

clear_previous_screenshots(true)

override_status_bar(true)

# Ensure mock data is seeded for every device (iPhone + iPad)
launch_arguments(["-SCREENSHOT_MODE"])

# Derived data to keep builds fast
derived_data_path("DerivedData")
